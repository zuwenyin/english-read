---
name: create-agents-md
overview: 在项目根目录创建 AGENTS.md 文件，用于约束 AI Agent 行为规则
todos:
  - id: create-agents-md
    content: 在根目录创建 AGENTS.md 文件，定义 agent 行为约束规则
    status: completed
---

## 用户需求

在 `d:/TraeWorkSpace/english-read/` 根目录下创建一个 `AGENTS.md` 文件，用于约束 agent 行为规则。

## 核心功能

- 创建 AGENTS.md 文件，定义 agent 行为约束规则
- 核心约束：遇到不清楚的问题必须跟用户沟通，不能自主决定
- 文件内容应清晰、结构化，便于 agent 阅读和遵循
